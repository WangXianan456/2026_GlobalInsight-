apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-monitor-dashboard
  namespace: global-insight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-monitor
  template:
    metadata:
      labels:
        app: data-monitor
    spec:
      containers:
      - name: monitor
        image: global-insight.tencentcloudcr.com/app/app:latest
        imagePullPolicy: Always
        command: ["java", "-cp", "/app/GlobalInsight.jar", "DataMonitorApp"]
        env:
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"

